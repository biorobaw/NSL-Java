# SCCS %W% -- %G% --%U%
#
# Dart model
#

proc nslMain {protocol} {
 
    nsl set system.protocol $protocol
       	
    puts "Training and Run model"
    nsl trainAndRunAll
 
    
   
} 

# protocols: { basic, transfer, calibration }.

nslMain basic

